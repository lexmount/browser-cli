interface:
  display_name: "Lexmount Browser CLI"
  short_description: "Control Lexmount browsers from Codex"
  default_prompt: "Use $browser-cli to open a Lexmount browser session, inspect a page, and return structured JSON results."
