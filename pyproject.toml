[project]
name = "browser-cli"
version = "0.3.23"
description = "Standalone CLI for operating Lexmount browser sessions"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "jq>=1.12.0",
    "lexmount>=0.5.14",
    "playwright>=1.40",
    "pydantic>=2.7",
    "pyyaml>=6.0",
    "regex>=2024.11.6",
    "websocket-client>=1.8",
    "websockets>=15.0.1",
]

[project.scripts]
browser-cli = "browser_cli.cli:main"

[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["browser_cli*", "lex_browser_runtime*"]

[tool.setuptools]
include-package-data = false

[tool.setuptools.package-data]
browser_cli = [
    "agent_skill/*.md",
    "agent_skill/scripts/*.py",
    "agent_references/*.md",
    "agent_examples/*.md",
    "agent_examples/cases/*.yaml",
    "agent_metadata/*.yaml",
]
lex_browser_runtime = [
    "registry/data/gov_portals.json",
    "registry/data/site_hints.yaml",
    "registry/data/adapters/*.json",
]

[dependency-groups]
dev = [
    "jq>=1.12.0",
    "pytest>=8.0",
    "ruff>=0.8",
    "websockets>=15.0.1",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
line-length = 88

[tool.ruff.lint]
# Keep Ruff's historical default lint contract explicit across Ruff releases.
select = ["E4", "E7", "E9", "F"]
