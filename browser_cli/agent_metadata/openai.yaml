interface:
  display_name: "Lexmount Browser CLI"
  short_description: "Control Lexmount sessions with bundled ACE page operations"
  default_prompt: "Use $browser-cli to manage Lexmount authentication and sessions, initialize the bundled ACE client with --lexmount-session-id for routine page work, reserve browser-cli action for a specialized fallback, and keep secrets out of chat."
