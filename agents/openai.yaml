interface:
  display_name: "Lexmount Browser CLI"
  short_description: "Control Lexmount browsers from Codex"
  default_prompt: "Use $browser-cli to discover commands, check auth status/refresh, run doctor or smoke-session checks, pick a context/session, and automate browser actions."
