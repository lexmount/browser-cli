interface:
  display_name: "Lexmount Browser CLI"
  short_description: "Control Lexmount browsers from Codex"
  default_prompt: "Use $browser-cli to inspect workflow contracts with commands --workflow, verify auth with auth status/refresh/login, run doctor --json before browser work, inspect commands before custom JavaScript, dry-run or pick persistent contexts, operate sessions/actions with JSON output, and keep secrets out of chat."
